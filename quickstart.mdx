---
title: 'Quickstart'
---

### Getting Started

1. **Install `uv`**

   If `uv` is not already installed, you can install it using the following command:

   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

2. **Clone the Repository**

   Clone the repository and navigate into it:

   ```bash
   git clone https://github.com/dr2633/Chronologue.git
   cd chronologue
   ```

3. **Set Up the Environment**

   Initialize the project using `uv`:

   ```bash
   uv init
   uv venv
   source .venv/bin/activate
   ```

4. **Add Core Dependencies**

 Install from `requirements.txt`

```bash
uv pip install -r requirements.txt
```

5. **Set Up MCP and API Keys**

Chronologue supports memory-grounded planning using the `mcp` agent CLI and external calendar APIs.

```bash
uv add "mcp[cli]" httpx
```

If installing locally with calendar sync 

```bash
brew install node
```

Run MCP Integration 

```bash
uv run mcp
```

Configure API Keys

Create a .env file in the root directory and populate it with your keys:

```bash
OPENAI_API_KEY=your-openai-key
CLAUDE_API_KEY=your-anthropic-key
GEMINI_API_KEY=your-gemini-key
LLAMA_API_KEY=your-llama-key

GOOGLE_CLIENT_CREDENTIALS=calendar/credentials.json
GOOGLE_TOKEN_FILE=calendar/token.json
```


6. **Run Test Scripts**

 Run the following script to transform `.json` data to `.ics`:

   ```bash
   python modules/export_ics.py
   ```

   Run this script to transform `.ics` to `.json`: 

   ```bash
   python modules/import_ics.py
   ```


## Get Started with Integrations and Applications 

Add content directly in your files with MDX syntax and React components. You can use any of our components, or even build your own.


<CardGroup>

<Card title="Google Calendar Sync" icon="calendar" href="/concepts/google-calendar">
  Set up Google Calendar synchronization.
</Card>

<Card title="iCalendar Sync" icon="calendar" href="/integrations/icalendar">
  Set up iCalendar synchronization. 
</Card>

<Card title="MCP Integration" icon="integration" href="/concepts/mcp-integration">
 Integrate with Model Context Protocol (MCP) server. 
</Card>

<Card title="Datasets" icon="calendar-2" href="/examples/datasets">
  Explore sample datasets and example workflows and schema. 
</Card>

</CardGroup>
